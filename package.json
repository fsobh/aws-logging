{
  "name": "aws-logging",
  "version": "1.1.8",
  "description": "Logs activity across all services under a single AWS account. Optional features can be enabled to trigger Email notifications based on the logs severity level",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/fsobh/aws-logging.git"
  },
  "author": "fsobh",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.1079.0",
    "short-unique-id": "^4.4.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
